# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  hello: "Hello world"
  login: 'log in'
  logoff: 'log off %{user}'
  books:
    language:
        pt_br: Portuguese
        en: English
    actions:
        action: Action
        send_to: Send to...
        edit: Edit
        delete: Delete
    requests: Requests
    no_requests: No requests
    request: Request
    requests_from: Requests from 
  requests:
    no_available: No requests available
  users:
      not_found: User not found
  activerecord:
    models:
      book: Book
      request: Request
      user: User
    attributes:
      book:
        author: Author
        title: Title
        language: Language
        created_at: Date
      request:
        created_at: Date
      user:
        username: Username
  time:
    formats:
      default: ! '%m/%d/%Y %H:%M'
